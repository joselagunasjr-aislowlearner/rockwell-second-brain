{
  "name": "rockwell-sync-scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "tsx sync.ts",
    "sync:drive": "tsx sync.ts --drive-only",
    "sync:gmail": "tsx sync.ts --gmail-only",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.55.0",
    "@supabase/supabase-js": "^2.38.4",
    "dotenv": "^16.4.0",
    "google-auth-library": "^9.0.0",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.ts"],
    "moduleFileExtensions": ["ts", "js", "json"]
  }
}
